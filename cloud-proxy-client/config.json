{
  "name": "Cloud Proxy Client (TM)",
  "version": "1.1.14",
  "slug": "cloud-proxy-client",
  "description": "Удаленный доступ для HA (Локальный репозиторий)",
  "arch": ["aarch64", "amd64"],
  "startup": "services",
  "boot": "auto",
  "options": {
    "token": ""
  },
  "schema": {
    "token": "str"
  }
}
